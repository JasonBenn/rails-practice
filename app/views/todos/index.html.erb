<%= link_to "New", new_todo_path %>
<ul>
  <% @todos.each do |todo| %>
    <li>
      <strong><%= todo.title %>:</strong> 
      <%= todo.description %> 
      <%= link_to "Show", todo %>
      <%= link_to "Edit", edit_todo_path(todo) %>
    </li>
  <% end %>
</ul>
